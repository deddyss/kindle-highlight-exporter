import chalk from "chalk";

const info = `Please have your Amazon username (${chalk.bold.yellow("email")}) and `
 + `${chalk.bold.yellow("password")} before proceeding in case you're not authorized to access Amazon, yet.`;

export default info;
